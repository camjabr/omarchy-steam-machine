#!/bin/sh
# Kept as a no-op safety net. On Wi-Fi, NetworkManager clears routes before
# pre-down.d runs, so SSAP cannot work here. TV power-off for sleep is handled
# by console-mode-tv-sleep.service, which watches the logind Suspend method
# call (before PrepareForSleep) via dbus BecomeMonitor.

exit 0
